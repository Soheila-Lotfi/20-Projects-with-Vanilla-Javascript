

const words = ['apple', 'udemy', 'programming', 'interface']
const selectedWord = words[Math.floor(Math.random() * words.length)]


window.addEventListener('keydown', e => {

    if (e.keyCode >= 65 && e.keyCode <= 90) {

        const letter = e.key.toLowerCase();
    }


})